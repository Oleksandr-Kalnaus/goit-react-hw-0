import styles from "./Options.module.css";

const Options = ({ data }) => {
  return (
    <div>
      <button>Good</button>
      <button>Neutral</button>
      <button>Bad</button>
      <button>Reset</button>
    </div>
  );
};

export default Options;
